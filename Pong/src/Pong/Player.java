package Pong;
//An enumeration in Java is a special data type which contains a set of predefined constants.
//You'll usually use an enum when dealing with values that aren't required 
//to change, like days of the week, seasons of the year, colors, and so on
public enum Player {
One,
Two
}

